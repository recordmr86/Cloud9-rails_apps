<%= form_for(@articles3) do |f| %>
  <% if @articles3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@articles3.errors.count, "error") %> prohibited this articles3 from being saved:</h2>

      <ul>
      <% @articles3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
